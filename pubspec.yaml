name: ui_rentmything
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^11.0.0
  # Cupertino icons for iOS style
  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.5

  # ✅ Add http package (latest stable v0.13.x)
  http: ^0.13.6

  # Add Dio for advanced HTTP (useful for web withCredentials and interceptors)
  dio: ^5.9.0

  # Secure storage for tokens on mobile
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/